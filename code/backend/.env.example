# DATABASE_URL points to PostgreSQL; required.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable
# PORT is HTTP listen port; runtime injects it.
PORT=8080
# APP_PORT is optional fallback when PORT is unset.
APP_PORT=8080
